import sys
input = sys.stdin.readline

for t in range(int(input())):
    N, Q = map(int, input().rstrip().split())
    for n in range(N):
        A, S = input().split()
    


    print(f"Case #{t+1}: {y} {z}/{w}")